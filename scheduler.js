// var scheduler = require("node-schedule");
// var rule = new scheduler.RecurrenceRole();
// rule.hour = 7
// rule.dayOfWeek = new.schedule.Range(0, 6)
// var dailyJob = schedule.scheduleJob(date, function() {
//     console.log()
// })

// /// do a post with req.body = for hour minute and dayOfWeek.

// // add to messages table as an array reminder?

// // set up so that they can do either one reminder or a recurring reminder on the same day every week.

// //

// module.exports = scheduler;
